<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>404 - Page Not Found</title>
    <link rel="stylesheet" href="/css/index.css"/>
    <!-- Load Tailwind CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css">
</head>
<body class="text-white h-screen">
<nav class="shadow absolute top-0 left-0">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-between py-4">
            <div class="flex items-center">
                <a href="/">
                    <img class="h-[10vh]" src="/svg/EZ" alt="Logo"/>
                </a>
            </div>
        </div>
    </div>
</nav>
<div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
    <h1 class="text-7xl font-bold text-white mb-4">
        <% if (typeof code !== 'undefined') { %>
            <%= code; %>
        <% } else { %>
            404
        <% } %>
        - Page Not Found</h1>

    <% if (typeof cause !== 'undefined') { %>
        <p class="text-gray-400 min-w-[11rem] animate-pulse text-4xl"><%= cause; %></p>
    <% } else { %>
        <p class="text-gray-400 text-4xl">Sorry, the page you are looking for could not be found.</p>
    <% } %>
</div>
</body>
</html>
