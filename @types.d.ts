declare module 'tiktok-video-downloader' {
	function getInfo(url: string): Promise<any>; // Replace `any` with the specific return type

	// Add any additional functions, interfaces, or types that are part of the module
	// Example:
	// interface TikTokVideo {
	//   id: string;
	//   title: string;
	//   // ...
	// }
}